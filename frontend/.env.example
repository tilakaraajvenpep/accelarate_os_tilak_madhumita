VITE_API_URL=

# Subdomain-based tenant routing base domain — must match the backend's
# BASE_DOMAIN. In dev this is "localhost" (subdomains resolve automatically
# per RFC 6761, e.g. acme.localhost:5173, admin.localhost:5173).
VITE_BASE_DOMAIN=localhost

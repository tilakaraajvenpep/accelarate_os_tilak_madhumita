export const PASSWORD_PATTERN = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^A-Za-z0-9]).{8,}$/

export const PASSWORD_HINT =
  'Must be at least 8 characters, with 1 uppercase letter, 1 special character, and a mix of lowercase letters and numbers.'
